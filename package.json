{
  "name": "e-voice",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix src-frontend",
    "build": "npm run build --prefix src-frontend",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
